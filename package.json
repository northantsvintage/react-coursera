{
  "name": "git-test",
  "version": "1.0.0",
  "description": "test directory for node and npm",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ida",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.4.0"
  }
}
